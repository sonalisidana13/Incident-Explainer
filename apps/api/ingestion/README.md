# Ingestion

Place ingestion jobs and one-off scripts here (e.g. chunking docs, generating embeddings, and loading `documents`/`chunks`).

Suggested entrypoints:
- `ingest-docs.js` for batch document ingestion
- `seed-incidents.js` for local demo incident packs
